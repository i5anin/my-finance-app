{
  "scripts": {
    "start_server": "npx json-server --watch db.json",
    "start": "npx json-server --watch db.json & browser-sync start --server --files \"*.html, *.css, *.js\""
  },
  "devDependencies": {
    "browser-sync": "^2.26.16"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
